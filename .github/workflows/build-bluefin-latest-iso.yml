name: Crow-OS ISO Build
on:
  workflow_dispatch:

jobs:
  build-latest:
    name: crow-os
    uses: ./.github/workflows/reusable-build-iso.yml
    secrets: inherit
    with:
      brand_name: crow-os
      fedora_version: 40
